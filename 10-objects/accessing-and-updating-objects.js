// Dot notation

// Accessing objects
const person = {
  name: "Thanos",
  age: 28,
};

// Accessing nested objects
const options = {
  color: "red",
  size: {
    width: 100,
    height: 200,
  },
};

// Bracket notation

// Destructuring and rest operator
