// Write a for loop that will iterate from 0 to 15. For each iteration, it will check if the current number is odd or even, and display a message to the screen.

/**
 * Rewrite the following example using a while and do while loops
 * let sum = 0;​
 * for (x = 1; x <= 10; x++) {​
 *    sum += Math.pow(x,2);​
 * }
 */

// Write a program which computes the average marks. Then, use this average to determine the corresponding grade.
// Ranges <60 -> F, <70 -> D, <80 -> C, <90 -> B, <100 -> A
const students = [
  ["David", 80],
  ["Vinoth", 77],
  ["Divya", 88],
  ["Ishitha", 95],
  ["Thomas", 68],
];
