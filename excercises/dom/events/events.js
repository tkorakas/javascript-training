// Create quereSelector and addEventListener to capture clicks from those 2 buttons and update count value accordingly.
// Counter id -> #counter
// Increament button id -> #increase
// Decrement button id -> #decrease

let count = 0;

function increase() {}

function decrease() {}
